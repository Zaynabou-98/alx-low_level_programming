#include "main.h"
/**
 * mul -  function that multiplies two integers
 * @a: print int to be multiplied to b
 * @b: print int to be multiplied to a
 * Return: Always 0.
 */

int mul(int a, int b)
{
return (a * b);
}

